import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const AlertOrder = () => {
  return (
    <View>
      <Text>Halo Alert Order</Text>
    </View>
  );
};

export default AlertOrder;

const styles = StyleSheet.create({});
